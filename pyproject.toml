[project]
name = "cinematch-ai"
version = "1.0.0"
description = "Motor de Recomendación Cinematográfica Conversacional"
requires-python = ">=3.8"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
